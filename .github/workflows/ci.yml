name: CI/CD Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  helia-core-tester:
    uses: ./.github/workflows/helia-core-tester.yml

  cortex-fvp-test-matrix:
    uses: ./.github/workflows/cortex-fvp-test-matrix.yml
