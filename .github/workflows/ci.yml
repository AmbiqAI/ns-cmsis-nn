name: CI/CD Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  operator-matrix-tests:
    uses: ./.github/workflows/operator-matrix-tester.yml

  helia-core-tester:
    uses: ./.github/workflows/helia-core-tester.yml
