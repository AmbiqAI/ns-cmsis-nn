name: CI/CD Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  unit-tests:
    uses: ./.github/workflows/reusable-unittest.yml

  helia-core-tester:
    uses: ./.github/workflows/reusable-helia-core-tester.yml
