---
title: API
description: The heliaCORE C API, grouped by operator family.
---

import Card from '@ambiqai/helia-ui/astro/Card';
import CardContent from '@ambiqai/helia-ui/astro/CardContent';
import CardGrid from '@ambiqai/helia-ui/astro/CardGrid';
import CardHeader from '@ambiqai/helia-ui/astro/CardHeader';
import LinkCard from '@ambiqai/helia-ui/astro/LinkCard';

heliaCORE keeps the CMSIS-NN C API shape where compatibility matters, then adds
Ambiq-tuned kernels and integration paths for HELIA workloads on Apollo-class
targets. Start with the operator families below, filter to the kernels you care
about, then drill into exact generated signatures when you need implementation
detail.

<CardGrid columns={3}>
  <LinkCard href="../why/" title="What it is" eyebrow="positioning">
    A CMSIS-NN-compatible kernel layer packaged for Ambiq firmware and HELIA runtime integration.
  </LinkCard>
  <LinkCard href="../why/#what-stays-compatible" title="How it differs" eyebrow="compatibility">
    Inherited CMSIS-NN APIs stay recognizable while Ambiq coverage fills model-graph gaps around real deployments.
  </LinkCard>
  <LinkCard href="../getting-started/neuralspot-x/" title="How it fits HELIA" eyebrow="integration">
    Use heliaCORE through neuralSPOT-X or direct CMake/CMSIS-Pack flows depending on your firmware stack.
  </LinkCard>
</CardGrid>

## Browse By Kernel Family

<CardGrid columns={3}>
  <Card>
    <CardHeader titleAs="h3" eyebrow="compute"><span>Convolution</span></CardHeader>
    <CardContent>Conv2D, depthwise, transpose convolution, wrappers, and buffer helpers.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="compute"><span>Fully connected</span></CardHeader>
    <CardContent>Dense layers, batch matmul paths, and scratch sizing helpers.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="graph glue"><span>Elementwise</span></CardHeader>
    <CardContent>Add, sub, mul, square difference, min/max, batch norm, select, and arithmetic glue.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="selection"><span>Reduction and comparison</span></CardHeader>
    <CardContent>Argmin/argmax, min/max reductions, comparisons, means, vector sums, and where.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="activation"><span>Activation</span></CardHeader>
    <CardContent>ReLU, LeakyReLU, PReLU, Hard-Swish, Logistic, Tanh, and clamp.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="layout"><span>Data movement</span></CardHeader>
    <CardContent>Pad, reshape, transpose, concatenate, gather, resize, strided slice, tile, broadcast, scatter, mirror pad, and sequence/slice-update utilities.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="tail"><span>Pooling, softmax, quantization</span></CardHeader>
    <CardContent>Classifier tail APIs plus dtype conversion and requantization utilities.</CardContent>
  </Card>
  <Card>
    <CardHeader titleAs="h3" eyebrow="sequence"><span>Sequence</span></CardHeader>
    <CardContent>LSTM, GRU, and SVDF functions for temporal workloads.</CardContent>
  </Card>
  <LinkCard href="api/" title="Types and support" eyebrow="index">
    Structs, enums, files, and low-level support helpers in the full generated index.
  </LinkCard>
</CardGrid>

For file-by-file browsing, structs, enums, and the complete generated tree, open
the [generated C API reference](api/).

## Types and Support

Structs, enums, files, and lower-level support helpers are intentionally kept in
the [full generated C API index](api/) to avoid
duplicating declarations across the grouped operator view and the complete
Exhale tree.
